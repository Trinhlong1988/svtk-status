[cosmic-ray]
module-path = "cmd_place.py"
timeout = 60.0
excluded-modules = []
test-command = "python -m pytest mutmut_target/test_core.py -x --tb=no -q"

[cosmic-ray.distributor]
name = "local"

[cosmic-ray.interceptors]
enabled = ["pragma_no_mutate"]
